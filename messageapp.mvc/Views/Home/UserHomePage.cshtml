<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professional Chatboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <style>
        /* --- GLOBAL STYLES --- */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f2f5;
            display: flex;
            height: 100vh;
        }

        /* --- SIDEBAR HEADER (Corrected Section) --- */
        .sidebar-header { /* Renamed from top-sidebar-header to integrate better */
            padding: 15px 20px;
            background-color: #075e54; /* Darker header color for contrast */
            display: flex;
            justify-content: space-between; /* Pushes 'Chats' left, icon right */
            align-items: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .profile-icon {
            color: white;
            font-size: 1.4em;
            cursor: pointer;
            padding: 5px;
            border-radius: 50%;
            transition: background-color 0.2s;
        }

            .profile-icon:hover {
                background-color: rgba(255, 255, 255, 0.2);
            }

        .header-title { /* Renamed from top-header-title */
            color: white;
            font-size: 1.2em;
            font-weight: 600;
        }

        /* --- USER LIST (Sidebar) --- */
        .sidebar {
            width: 300px;
            background-color: #ffffff;
            border-right: 1px solid #ddd;
            /* ⚠️ CHANGED: Removed overflow-y: auto and padding-top to fix layout */
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.05);
            display: flex; /* Make it a flex container */
            flex-direction: column; /* Stack header and list vertically */
        }

        .chats-list-container {
            flex-grow: 1; /* Allows the list to take up all remaining space */
            overflow-y: auto; /* Adds scrollbar only to the chat list, not the header */
        }

        .sidebar h2 {
            /* Kept in CSS but assume it's removed from HTML, or hidden */
            padding: 0 20px 10px;
            color: #128c7e;
            border-bottom: 1px solid #eee;
            margin-top: 0;
            display: none; /* Hide the old H2 tag */
        }

        #usersList {
            list-style: none;
            padding: 0;
            margin: 0;
        }

            #usersList li {
                padding: 15px 20px;
                border-bottom: 1px solid #eee;
                transition: background-color 0.2s;
                cursor: pointer;
                display: flex;
                flex-direction: column;
            }

                #usersList li:hover {
                    background-color: #f7f7f7;
                }

                #usersList li strong {
                    font-weight: 600;
                }

        .user-status {
            font-size: 12px;
            margin-top: 4px;
            color: #666;
        }

        /* --- CHAT AREA --- */
        .chat-area {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            background-color: #e5ddd5;
        }

        .chat-header {
            padding: 15px 20px;
            background-color: #ffffff;
            border-bottom: 1px solid #ddd;
            box-shadow: 0 2px 2px rgba(0, 0, 0, 0.05);
            font-size: 1.2em;
            font-weight: 600;
            color: #333;
        }

        #chatBox {
            flex-grow: 1;
            overflow-y: auto;
            padding: 20px 10px;
        }

        /* Message Styling */
        .message-container {
            overflow: hidden;
            width: 100%;
        }

        .message-bubble {
            max-width: 70%;
            padding: 10px 15px;
            border-radius: 8px;
            margin: 5px 10px;
            position: relative;
            box-shadow: 0 1px 0.5px rgba(0, 0, 0, 0.13);
            font-size: 14px;
            word-wrap: break-word;
        }

        /* Your Message */
        .message-you {
            background-color: #dcf8c6;
            float: right;
            clear: both;
        }

        /* Other User's Message */
        .message-other {
            background-color: #ffffff;
            float: left;
            clear: both;
        }

        /* Sender Name */
        .message-sender {
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
            color: #075e54;
        }

        /* Time Stamp */
        .message-time {
            display: block;
            text-align: right;
            font-size: 10px;
            color: #777;
            margin-top: 5px;
            line-height: 1;
        }

        /* --- INPUT AREA --- */
        .chat-input-area {
            padding: 15px 20px;
            background-color: #f0f0f0;
            border-top: 1px solid #ddd;
            display: flex;
        }

        #privateMessageInput {
            flex-grow: 1;
            padding: 10px 15px;
            border: 1px solid #ccc;
            border-radius: 20px;
            margin-right: 10px;
            font-size: 1em;
        }

        .send-button {
            padding: 10px 20px;
            background-color: #128c7e;
            color: white;
            border: none;
            border-radius: 20px;
            cursor: pointer;
            transition: background-color 0.2s;
            font-weight: 600;
        }

            .send-button:hover {
                background-color: #0c7a6e;
            }
    </style>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.5/signalr.min.js"></script>
</head>
<body>

    <div class="sidebar" id="sidebar">

        <div class="sidebar-header">
            <span class="header-title">Concord Chat</span>
            <i class="fas fa-user-circle profile-icon" title="Go to Profile" onclick="goToProfile()"></i>
        </div>

        <div class="chats-list-container">
            <ul id="usersList">
            </ul>
        </div>
    </div>

    <div class="chat-area">
        <div class="chat-header">
            <span id="chatWith">Select a user to chat</span>
        </div>

        <div id="chatBox">
        </div>

        <div class="chat-input-area">
            <input id="privateMessageInput" placeholder="Type your message..." />
            <button class="send-button" onclick="sendPrivateMessage()">Send</button>
        </div>
    </div>

    <script>
        // Placeholder variables (You should load these from your JWT/Authentication)
        // const myEmail = "user@example.com";
        // const myUserId = "some-guid-123";

        // function goToProfile() {
        //     alert(`
        //         👤 User Profile:
        //         Email: ${myEmail}
        //         User ID: ${myUserId}
        //         ---
        //         (In a real application, this would navigate to the profile management page.)
        //     `);
        // }

        // Placeholder functions for the rest of the chat logic
        // function sendPrivateMessage() {
        //     alert("Send message function triggered. (Requires SignalR/Backend logic)");
        // }

        // You need to replace the following two script tags with actual content/links
        // This part was duplicated and incorrect in your original submission. 
     <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.5/signalr.min.js"> </script>
     <script src="~/js/userauth/userhomepage.js"></script>

    </script>
</body>
</html>